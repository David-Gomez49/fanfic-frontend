# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: auth.spec.ts >> Auth flows >> register a new user
- Location: e2e\auth.spec.ts:4:3

# Error details

```
Test timeout of 30000ms exceeded.
```

```
Error: expect(locator).toBeVisible() failed

Locator: locator('a[aria-label="Profile"]')
Expected: visible
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 10000ms
  - waiting for locator('a[aria-label="Profile"]')

```

```yaml
- region "Notifications alt+T"
- dialog "Welcome":
  - heading "Welcome" [level=2]
  - paragraph: Sign in or create an account
  - tablist:
    - tab "Sign in"
    - tab "Sign up" [selected]
  - tabpanel "Sign up":
    - text: Username
    - textbox "Username":
      - /placeholder: your_username
      - text: e2enew_1784428104965
    - text: Password
    - textbox "Password":
      - /placeholder: ••••••••
      - text: NewTestPass1
    - button
    - text: Confirm password
    - textbox "Confirm password":
      - /placeholder: ••••••••
      - text: NewTestPass1
    - button
    - text: Minimum 8 characters At least one uppercase letter At least one lowercase letter At least one number
    - button [disabled]
  - button "Close"
```

# Test source

```ts
  1  | import { test, expect, login, logout } from './helpers'
  2  | 
  3  | test.describe('Auth flows', () => {
  4  |   test('register a new user', async ({ page }) => {
  5  |     const ts = Date.now()
  6  |     const username = `e2enew_${ts}`
  7  |     const password = 'NewTestPass1'
  8  | 
  9  |     await page.goto('/')
  10 | 
  11 |     const signInButton = page.locator('header button', { hasText: 'Sign in' })
  12 |     await signInButton.click()
  13 | 
  14 |     await page.getByRole('tab', { name: 'Sign up' }).click()
  15 | 
  16 |     await page.locator('#su-u').fill(username)
  17 |     await page.locator('#su-p').fill(password)
  18 |     await page.locator('#su-cp').fill(password)
  19 | 
  20 |     await page.getByRole('dialog').getByRole('button', { name: 'Sign up' }).click()
  21 | 
> 22 |     await expect(page.locator('a[aria-label="Profile"]')).toBeVisible({ timeout: 10000 })
     |                                                           ^ Error: expect(locator).toBeVisible() failed
  23 |   })
  24 | 
  25 |   test('login and logout with existing user', async ({ page, testData }) => {
  26 |     await login(page, testData.user.username, testData.user.password)
  27 | 
  28 |     await expect(page.locator('a[aria-label="Profile"]')).toBeVisible()
  29 | 
  30 |     await logout(page)
  31 | 
  32 |     await expect(page.getByRole('button', { name: /sign in/i })).toBeVisible()
  33 |   })
  34 | 
  35 |   test('invalid credentials show error', async ({ page }) => {
  36 |     await page.goto('/')
  37 | 
  38 |     await page.locator('header button', { hasText: 'Sign in' }).click()
  39 | 
  40 |     await page.locator('#li-u').fill('nonexistent_user')
  41 |     await page.locator('#li-p').fill('WrongPass1')
  42 |     await page.getByRole('dialog').getByRole('button', { name: 'Sign in' }).click()
  43 | 
  44 |     await expect(page.getByText(/invalid credentials/i)).toBeVisible()
  45 |   })
  46 | })
  47 | 
```